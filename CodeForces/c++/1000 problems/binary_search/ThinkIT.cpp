#include <vector>
#include "iostream"

using namespace std;

int main() {
    int n;
    cout << "Enter the number of participants: ";
    cin >> n;

    for (int i=0;i<n; i++) {
        string name;
        int timezone;
        vector<pair<int, int>> availabilies;
        cout << "Enter the name of participant: ";
        cin >> name;
        cout << "Enter the timezone of participant: ";
        cin >> timezone;
        cout << "Enter the availabilities of participant: ";
        cin >> ;

    }
}
